@model Category

<h2>Delete Category</h2>

<p>Are you sure you want to delete this category?</p>
<p><strong>@Model.CategoryId</strong></p>
<p><strong>@Model.CategoryName</strong></p>

@* <form method="post" action="@Url.Action("Delete", new { id = Model.CategoryId })">
    <button type="submit" class="btn btn-danger">Delete</button>
    <a href="@Url.Action("Index")" class="btn btn-secondary">Cancel</a>
</form> *@


<form method="post" action="@Url.Action("Delete", "Category", new { id = Model.CategoryId })">
    @Html.AntiForgeryToken()
    <button type="submit" class="btn btn-danger">Delete</button>
    <a href="@Url.Action("Index")" class="btn btn-secondary">Cancel</a>
</form>
